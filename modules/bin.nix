{
  home.file."bin/helix-live-grep" = {
    source = ./bin/helix-live-grep;
  };
  home.file."bin/live-grep" = {
    source = ./bin/live-grep;
  };
  home.file."bin/helix-git-blame" = {
    source = ./bin/helix-git-blame;
  };
}